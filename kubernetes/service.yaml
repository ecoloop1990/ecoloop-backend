apiVersion: v1
kind: Service
metadata:
  name: ecoloop-backend
  namespace: default
  labels:
    app: ecoloop-backend
spec:
  type: ClusterIP
  selector:
    app: ecoloop-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
